import { module, test } from 'qunit';
import { setupTest } from 'frontend/tests/helpers';

module('Unit | Route | thirteen/pt3', function (hooks) {
  setupTest(hooks);

  /*
  test('it exists', function (assert) {
    let route = this.owner.lookup('route:thirteen/pt3');
    assert.ok(route);
  });
  */
});
